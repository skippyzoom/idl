pro project_graphics_space_diff, context
  print, "PROJECT_GRAPHICS_SPACE_DIFF: Not available yet."
  ;; background = rms(imgdata[*,*,0:3],dim=3)
  ;; for it=0,context.params.nt_max-1 do imgdata[*,*,it] -= background
end
